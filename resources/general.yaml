name: "hMusic"
version: "1.0"

monolog:
  monolog.level: 300
  monolog.name: application

database:
  default:
    db.options:
      driver: pdo_mysql
      host: localhost
      dbname: hmusic
      user: root
      password: simple
      charset: utf8

imports:
  - { resource: routes.yaml }
